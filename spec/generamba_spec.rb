require 'spec_helper'

describe Generamba do
  it 'has a version number' do
    expect(Generamba::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
